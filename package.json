{
  "name": "blog-syndicator",
  "version": "0.1.0",
  "private": true,
  "description": "Detecta posts novos no RSS do blog, gera copy por rede social com IA e publica (com aprovação no Telegram).",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "*",
    "@atproto/api": "*",
    "dotenv": "*",
    "rss-parser": "*",
    "tsx": "*"
  }
}
